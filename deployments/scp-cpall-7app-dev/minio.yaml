# dev: no ingress (cluster-internal)
apiIngress:
  enabled: false
ingress:
  enabled: false
# bitnami purged public.ecr.aws/bitnami; use official legacy relocation
global:
  imageRegistry: docker.io
  imagePullSecrets:
    - docker-hub
image:
  repository: bitnamilegacy/minio
