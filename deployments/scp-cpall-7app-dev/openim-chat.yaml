# dev clone: fix service refs for this namespace, no ingress
config:
  mongo:
    address:
    - im-mongodb:27017
    uri: mongodb://openIM:openIM123@im-mongodb-0.im-mongodb-headless:27017/openim_v3?replicaSet=rs0&maxPoolSize=100
  redis:
    address:
    - im-redis-master:6379
  openIMUrl: http://openimserver-openim-api
# cluster-internal: disable both ingresses (admin-api top-level + chat-api)
ingress:
  enabled: false
chat-api:
  ingress:
    enabled: false
