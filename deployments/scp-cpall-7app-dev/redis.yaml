# dev: master only, no replicas
replica:
  replicaCount: 0
# bitnami purged public.ecr.aws/bitnami; use official legacy relocation
global:
  imageRegistry: docker.io
  imagePullSecrets:
    - docker-hub
image:
  repository: bitnamilegacy/redis
