version: v2beta1
name: sds-scp-scpap-pocgit
 
hooks:
  - command: antigravity --folder-uri vscode-remote://ssh-remote+openim-dev/app
    events: [ "after:initSync" ]
    background: true

# This is a list of `pipelines` that DevSpace can execute (you can define your own)
pipelines:
  # You can run this pipeline via `devspace deploy` (or `devspace run-pipeline deploy`)
  deploy:
    run: |-
      run_dependencies --all                            # 1. Deploy any projects this project needs (see "dependencies")
      ensure_pull_secrets --all                         # 2. Ensure pull secrets
      build_images --all -t $(git describe --always)    # 3. Build, tag (git commit hash) and push all images (see "images")
      create_deployments --all                          # 4. Deploy Helm charts and manifests specfied as "deployments"
  # This is the pipeline for the main command: `devspace dev` (or `devspace run-pipeline dev`)
  dev:
    run: |-
      run_dependencies --all       # 1. Deploy any projects this project needs (see "dependencies")
      ensure_pull_secrets --all    # 2. Ensure pull secrets
      create_deployments --all     # 3. Deploy Helm charts and manifests specfied as "deployments"
      start_dev app nginx-ingress chat-admin # 4. Start dev mode targets

# This is a list of `deployments` that DevSpace can create for this project
deployments:
  app:
    # This deployment uses `helm` but you can also define `kubectl` deployments or kustomizations
    helm:
      # We are deploying this project with the Helm chart you provided
      chart:
        name: component-chart
        repo: https://charts.devspace.sh
      # Under `values` we can define the values for this Helm chart used during `helm install/upgrade`
      # You may also use `valuesFiles` to load values from files, e.g. valuesFiles: ["values.yaml"]
      values:
        containers:
          - image: public.ecr.aws/docker/library/python:3.12.13-slim
            name: app
            workingDir: /app

# This is a list of `dev` containers that are based on the containers created by your deployments
dev:
  app:
    # Target the deployment created above
    labelSelector:
      app.kubernetes.io/name: devspace-app
    # Replace the container image with this dev-optimized image
    devImage: public.ecr.aws/docker/library/python:3.12.13-bookworm
    # Sync files between the local filesystem and the development container
    sync:
      - path: ../:/app
        onUpload:
          execRemote:
            command: sh
            args:
              - -c
              - "mkdir -p /app/scripts && chmod +x /app/scripts/*.sh 2>/dev/null || true"
    # Forward local ports for application access
    ports:
      - port: "8080:8080"
    # Open a terminal and use the following command to start it
    terminal:
      command: ./devspace/devspace_start.sh
    # Inject a lightweight SSH server into the container (so your IDE can connect to the remote dev env)
    ssh:
      enabled: true
      localHostname: openim-dev
    # Make the following commands from my local machine available inside the dev container
    proxyCommands:
      - command: devspace
      - command: kubectl
      - command: helm
      - gitCredentials: true

  nginx-ingress:
    labelSelector:
      app.kubernetes.io/name: ingress-nginx
      app.kubernetes.io/instance: openim-nginx
    namespace: openim
    ports:
      - port: "9090:80"
      - port: "9443:443"
  
  chat-admin:
    labelSelector:
      app.kubernetes.io/name: admin-api
      app.kubernetes.io/instance: openim-chat
    namespace: openim
    ports:
      - port: "10009:80"

# Use the `commands` section to define repeatable dev workflows for this project 
commands:
  migrate-db:
    command: |-
      echo 'This is a cross-platform, shared command that can be used to codify any kind of dev task.'
      echo 'Anyone using this project can invoke it via "devspace run migrate-db"'

# Define dependencies to other projects with a devspace.yaml
# dependencies:
#   api:
#     git: https://...  # Git-based dependencies
#     tag: v1.0.0
#   ui:
#     path: ./ui        # Path-based dependencies (for monorepos)
